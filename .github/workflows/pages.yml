name: Deploy Pacefold to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Reassemble tested release
        run: |
          cat pacefold-site.zip.part-* > pacefold-site.zip
          echo "94ece9e124a256cd3416297389b2899af9cb95e8cec51146140df466bf299a24  pacefold-site.zip" | sha256sum -c -
          unzip -q pacefold-site.zip -d _site
      - name: Validate static application
        run: |
          test -f _site/index.html
          test -f _site/privacy.html
          test -f _site/app/index.html
          test -f _site/app/app.css
          test -f _site/app/app.js
          test -f _site/app/service-worker.js
          test -f _site/app/manifest.webmanifest
          python -m json.tool _site/app/manifest.webmanifest > /dev/null
          node --check _site/app/app.js
          node --check _site/app/service-worker.js
      - name: Configure Pages
        uses: actions/configure-pages@v5
      - name: Upload static site
        uses: actions/upload-pages-artifact@v4
        with:
          path: _site
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
