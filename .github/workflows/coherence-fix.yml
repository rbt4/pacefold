name: Pacefold 25 coherence apply

on:
  push:
    branches:
      - agent/pacefold-25-coherence

permissions:
  contents: write

jobs:
  apply:
    if: "!contains(github.event.head_commit.message, '[coherence]')"
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: agent/pacefold-25-coherence
          fetch-depth: 0
      - name: Apply coherence source
        run: node scripts/apply-coherence.cjs
      - name: Verify source
        run: |
          node scripts/verify-canonical.cjs canonical
          node --check canonical/app/pacefold-v25-recovery.js
          node --check scripts/v25-coherence-audit.cjs
      - name: Commit transformed source
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add canonical/app/pacefold-v25-recovery.css canonical/app/pacefold-v25-recovery.js scripts/verify-canonical.cjs
          git diff --cached --quiet && exit 0
          git commit -m "Correct Pacefold 25 home composition [coherence]"
          git push origin HEAD:agent/pacefold-25-coherence
