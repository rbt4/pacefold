name: Agent materialize verified core

on:
  push:
    branches: [agent/pacefold-function-hardening]
    paths:
      - .github/workflows/agent-materialize.yml
      - .agent-materialize-trigger

permissions:
  contents: write

jobs:
  materialize:
    if: github.repository == 'rbt4/pacefold' && github.ref == 'refs/heads/agent/pacefold-function-hardening'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout isolated branch
        uses: actions/checkout@v6
        with:
          ref: agent/pacefold-function-hardening
          fetch-depth: 0
      - name: Reconstruct and verify sealed core
        run: |
          rm -rf _agent_core
          cat release/pacefold-v15.zip.b64.part-* | base64 --decode > /tmp/pacefold-v15.zip
          expected="$(sed -n 's/^- SHA-256: `\([a-f0-9]\{64\}\)`.*/\1/p' RELEASE.md)"
          test -n "$expected"
          echo "$expected  /tmp/pacefold-v15.zip" | sha256sum -c -
          mkdir _agent_core
          unzip -q /tmp/pacefold-v15.zip -d _agent_core
          find _agent_core -type f -print0 | sort -z | xargs -0 sha256sum > _agent_core/SHA256SUMS
      - name: Commit temporary working tree
        run: |
          git config user.name pacefold-agent
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -f _agent_core
          git commit -m "Materialize verified core for hardening" || exit 0
          git push origin HEAD:agent/pacefold-function-hardening
