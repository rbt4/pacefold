'use strict';
const fs=require('node:fs');

const corePath='canonical/app/pacefold-v25-core.js';
let core=fs.readFileSync(corePath,'utf8');
const apiOld="window.__PACEFOLD_WORKSPACE__={revision:REVISION,surfaceRelease:'25.0.0',reconcile:queue,readWorkWindow,openNotebook:()=>setNotebookOpen(true,false),closeNotebook:()=>setNotebookOpen(false,false,false),copyDay,player:{open:()=>setPlayerDrawer(true),close:()=>setPlayerDrawer(false),refresh:refreshTracks}};";
const apiNew="window.__PACEFOLD_WORKSPACE__={revision:REVISION,surfaceRelease:'25.0.0',reconcile:queue,readWorkWindow,addNote:(body,category='Daily')=>createEntry(body,category),openNotebook:()=>setNotebookOpen(true,false),closeNotebook:()=>setNotebookOpen(false,false,false),copyDay,player:{open:()=>setPlayerDrawer(true),close:()=>setPlayerDrawer(false),refresh:refreshTracks}};";
if(core.includes(apiOld))core=core.replace(apiOld,apiNew);else if(!core.includes("addNote:(body,category='Daily')=>createEntry(body,category)"))throw new Error('Canonical workspace API anchor missing');
fs.writeFileSync(corePath,core);

const privatePath='canonical/app/pacefold-v25-private.js';
let js=fs.readFileSync(privatePath,'utf8');
const sheetOld="const footer=create('div','pf25-private-composer-foot'),status=create('span','pf25-private-save-state',''),save=button('pf25-private-save','Save','');save.append(create('i','pf25-private-save-fold'));save.addEventListener('click',savePrivateNote);input.addEventListener('keydown',event=>{resetDaybookTimer();if((event.ctrlKey||event.metaKey)&&event.key==='Enter'){event.preventDefault();savePrivateNote()}});composer.append(input,footer);footer.append(status,save);";
const sheetNew="const footer=create('div','pf25-private-composer-foot'),status=create('span','pf25-private-save-state',''),save=button('pf25-private-save','Save','');save.append(create('i','pf25-private-save-fold'));save.addEventListener('click',savePrivateNote);input.addEventListener('keydown',event=>{resetDaybookTimer();if((event.ctrlKey||event.metaKey)&&event.key==='Enter'){event.preventDefault();savePrivateNote()}});for(const type of ['pointerdown','keydown','focusin'])sheet.addEventListener(type,event=>event.stopPropagation());composer.append(input,footer);footer.append(status,save);";
if(js.includes(sheetOld))js=js.replace(sheetOld,sheetNew);else if(!js.includes("for(const type of ['pointerdown','keydown','focusin'])sheet.addEventListener"))throw new Error('Private sheet event-isolation anchor missing');
const spineOld="spine.addEventListener('click',event=>{event.preventDefault();event.stopPropagation();toggleDaybook()});head.prepend(spine)";
const spineNew="spine.addEventListener('pointerdown',event=>event.stopPropagation());spine.addEventListener('keydown',event=>event.stopPropagation());spine.addEventListener('focusin',event=>event.stopPropagation());spine.addEventListener('click',event=>{event.preventDefault();event.stopPropagation();toggleDaybook()});head.prepend(spine)";
if(js.includes(spineOld))js=js.replace(spineOld,spineNew);else if(!js.includes("spine.addEventListener('pointerdown',event=>event.stopPropagation())"))throw new Error('Private spine event-isolation anchor missing');
const saveOld="function savePrivateNote(){\n  const input=id('pf25-private-daybook-input'),body=text(input?.value);if(!body)return;const now=new Date(),key=localKey(now),all=readNotes();const note={id:`fold-${now.getTime().toString(36)}-${Math.random().toString(36).slice(2,6)}`,date:key,body,category:'Daily',createdAt:now.toISOString(),updatedAt:now.toISOString()};\n  try{all.push(note);writeNotes(all);window.__PACEFOLD_DAYFLOW__?.add?.('note','Note captured',body.slice(0,72),now.getTime(),'note-fold');window.dispatchEvent(new CustomEvent('pacefold:storage-changed',{detail:{key:NOTES_KEY,source:'privacy-fold'}}));input.value='';setText(id('pf25-private-daybook-sheet')?.querySelector('.pf25-private-save-state'),'•');buildActivity(id('pf25-private-activity'));setTimeout(()=>{setText(id('pf25-private-daybook-sheet')?.querySelector('.pf25-private-save-state'),'');closeDaybook()},700)}catch{setText(id('pf25-private-daybook-sheet')?.querySelector('.pf25-private-save-state'),'×')}\n}";
const saveNew="function savePrivateNote(){\n  const input=id('pf25-private-daybook-input'),body=text(input?.value);if(!body)return;const now=new Date();\n  try{let saved=false;if(typeof window.__PACEFOLD_WORKSPACE__?.addNote==='function')saved=window.__PACEFOLD_WORKSPACE__.addNote(body,'Daily')===true;else{const key=localKey(now),all=readNotes();all.push({id:`fold-${now.getTime().toString(36)}-${Math.random().toString(36).slice(2,6)}`,date:key,body,category:'Daily',section:'Daily',createdAt:now.toISOString(),updatedAt:now.toISOString(),pinned:false,archived:false,completed:false,format:'markdown'});writeNotes(all);window.dispatchEvent(new CustomEvent('pacefold:storage-changed',{detail:{key:NOTES_KEY,source:'privacy-fold-fallback'}}));saved=true}if(!saved)throw new Error('Notebook writer rejected note');try{window.__PACEFOLD_DAYFLOW__?.add?.('note','Note captured',body.slice(0,72),now.getTime(),'note-fold')}catch{}input.value='';setText(id('pf25-private-daybook-sheet')?.querySelector('.pf25-private-save-state'),'•');buildActivity(id('pf25-private-activity'));setTimeout(()=>{setText(id('pf25-private-daybook-sheet')?.querySelector('.pf25-private-save-state'),'');closeDaybook()},700)}catch(error){try{window.__PACEFOLD_DIAGNOSTICS__?.recordError?.('private-note-save',error)}catch{}setText(id('pf25-private-daybook-sheet')?.querySelector('.pf25-private-save-state'),'×')}\n}";
if(js.includes(saveOld))js=js.replace(saveOld,saveNew);else if(!js.includes("window.__PACEFOLD_WORKSPACE__.addNote(body,'Daily')"))throw new Error('Private save anchor missing');
fs.writeFileSync(privatePath,js);
console.log('Private Daybook now uses the canonical note writer and isolates legacy touch events');
